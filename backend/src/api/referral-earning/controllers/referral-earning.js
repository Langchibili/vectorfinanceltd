'use strict';

/**
 * referral-earning controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::referral-earning.referral-earning');
