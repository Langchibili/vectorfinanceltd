'use strict';

/**
 * loans-information service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::loans-information.loans-information');
